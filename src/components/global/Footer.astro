---
import { site, partner } from '../../data/site';
const year = 2026;
---

<footer class="footer">
  <div class="container footer__inner">
    <div class="footer__top">
      <div class="footer__brand-col">
        <p class="footer__brand">Alexandru Cupsa</p>
        <p class="footer__tag">Soluzioni IT su misura — Web, Security, Database, Automation.</p>
      </div>

      <nav class="footer__nav" aria-label="Mappa del sito">
        <span class="footer__col-label">Sito</span>
        <a href="/PORTFOLIO_FREELANCE/#about">Chi Sono</a>
        <a href="/PORTFOLIO_FREELANCE/#servizi">Servizi</a>
        <a href="/PORTFOLIO_FREELANCE/projects/">Progetti</a>
        <a href="/PORTFOLIO_FREELANCE/#studio">Studio</a>
        <a href="/PORTFOLIO_FREELANCE/#contatti">Parliamo</a>
      </nav>

      <div class="footer__contact">
        <span class="footer__col-label">Contatti</span>
        <a href={`mailto:${site.email}`}>{site.email}</a>
        <a href={site.github} target="_blank" rel="noopener">GitHub</a>
        <span class="footer__muted">{site.location}</span>
      </div>

      <div class="footer__extra">
        <span class="footer__col-label">Studio</span>
        <a href={partner.portfolio} target="_blank" rel="noopener" class="footer__partner">
          {partner.name}
        </a>
        <span class="footer__muted">Design · Frontend · SEO</span>
      </div>
    </div>

    <div class="footer__base">
      <span>&copy; {year} Alexandru Cupsa</span>
      <span>Verona &middot; Italia</span>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--bg-secondary);
    border-top: 1px solid var(--border);
  }
  .footer__inner { padding: clamp(3rem, 2rem + 3vw, 5rem) 0 2.5rem; }

  .footer__top {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
    padding-bottom: 2.5rem;
    border-bottom: 1px solid var(--hairline);
  }

  .footer__brand {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--text-bright);
    letter-spacing: -0.02em;
    margin-bottom: 0.7rem;
  }
  .footer__tag {
    color: var(--text-secondary);
    font-size: var(--text-sm);
    line-height: 1.7;
    max-width: 34ch;
  }

  .footer__nav, .footer__contact, .footer__extra {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }
  .footer__col-label {
    font-family: var(--font-mono);
    font-size: var(--text-micro);
    text-transform: uppercase;
    letter-spacing: 0.16em;
    color: var(--text-muted);
    margin-bottom: 0.3rem;
  }
  .footer__nav a, .footer__contact a {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    transition: color var(--dur);
    width: fit-content;
  }
  .footer__nav a:hover, .footer__contact a:hover { color: var(--accent-blue); }
  .footer__partner {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    transition: color var(--dur);
    width: fit-content;
  }
  .footer__partner:hover { color: var(--accent-green); }
  .footer__muted { font-size: var(--text-sm); color: var(--text-muted); }

  .footer__base {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 0.5rem;
    padding-top: 1.6rem;
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--text-muted);
    letter-spacing: 0.04em;
  }

  @media (min-width: 760px) {
    .footer__top { grid-template-columns: 1.6fr 1fr 1fr 1fr; gap: 2rem; }
  }
</style>
